pipeline {
    agent any
    
    stages {
        stage('Run Command') {
            steps {
                script {
                    "echo ${params.test} "
                }
            }
        }
    }
}

